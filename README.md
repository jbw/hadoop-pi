# hadoop-pi

### Install datanode

`./install.sh`

### Install namenode

`./install.sh MASTER`

### Configure hosts

In `hadoop-config-master`, modify `slaves` and `hosts` for your network configuration. 